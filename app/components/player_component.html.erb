<div class="space-y-2 flex flex-col">
  <div class="flex justify-between items-center space-x-2">
    <%= button_to active_list_entry_list_path(list),
     method: :patch,
     params: {list: {active_list_entry_id: previous_track&.listable&.id}},
     disabled: !previous_track.present?,
     class: button_klass do %>
      <i class="fas fa-backward-step fa-lg"></i>  
    <% end %>

    <%= audio_tag track.audio_url, controls: true, class: "flex-1", autoplay: true, loop: false %>

    <%= button_to active_list_entry_list_path(list),
     method: :patch,
     params: {list: {active_list_entry_id: next_track&.listable&.id}},
     disabled: !next_track.present?,
     class: button_klass do %>
      <i class="fas fa-forward-step fa-lg"></i>  
    <% end %>
  </div> 

  <div class="flex justify-around items-center space-x-4 w-1/2 self-center">
    <%= button_to toggle_repeat_list_path(list),
     method: :patch,
     class: button_klass(repeat) do %> 
      <i class="fas fa-repeat fa-lg"></i>
    <% end %>
    <%= button_to toggle_shuffle_list_path(list),
     method: :patch,
     class: button_klass(shuffle) do %> 
      <i class="fas fa-shuffle fa-lg"></i>
    <% end %>
  </div> 
</div>
