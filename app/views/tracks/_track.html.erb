<div class="space-y-2">
  <h3 class="text-lg leading-6 font-medium text-gray-900">
    <%= track.name %>
  </h3>

  <%= render(PlayerComponent.new(track: track, list: list, repeat: session[list.to_gid.to_s]["repeat"], shuffle: session[list.to_gid.to_s]["shuffle"])) %>
</div>

